import type { Metadata } from "next";
import { Halant } from "next/font/google";
import { Inter } from "next/font/google";
import "./globals.css";
import "@/lib/gsap-setup";
import { ServiceWrapper } from "@/components/ServiceWrapper";
import Tag from "@/tag/Tag";
import { getVisualEditScript } from "@/utils/visual-edit-script";
import { Manrope } from "next/font/google";



export const metadata: Metadata = {
  title: 'Super Boost Gym Dubai | Book Your Free Trial',
  description: 'Join Dubai\'s premier gym. Expert coaches, modern equipment, and results-driven training at Super Boost Gym in Al Barsha.',
  keywords: ["gym dubai, fitness al barsha, personal training dubai, super boost gym"],
  openGraph: {
    "title": "Super Boost Gym Dubai",
    "description": "Transform your body today at Super Boost Gym, Dubai's top rated fitness center.",
    "siteName": "Super Boost Gym"
  },
};

const manrope = Manrope({
  variable: "--font-manrope",
  subsets: ["latin"],
});

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <ServiceWrapper>
        <body className={`${manrope.variable} antialiased`}>
          <Tag />
          {children}
          <script
              dangerouslySetInnerHTML={{
                  __html: `${getVisualEditScript()}`
              }}
          />
        </body>
      </ServiceWrapper>
    </html>
  );
}
